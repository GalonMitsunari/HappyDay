{# templates/birthday/edit.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Modifier un anniversaire{% endblock %}

{% block body %}
    <h1>Modifier un anniversaire</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Modifier</button>
    {{ form_end(form) }}
{% endblock %}
